import React from 'react';

const Icon = props => (
  <svg viewBox="0 0 1024 1024" width="1em" height="1em" fill="currentColor">
    <path d={props.icon} />
  </svg>
);

export default Icon;
